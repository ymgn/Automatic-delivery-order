<h1>注文リストを新規作成</h1>

<%= simple_form_for @order_list, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name, label: "注文リスト名", error: '名前エラー(仮)'  %>
  <%= f.input :sort_num, label: "注文リスト固有番号", error: 'ソートかぶりエラー(仮)'%>

  <%= f.button :submit, "新規作成", :class => 'btn-primary' %>
<% end %>

<%= link_to 'Back', account_path(session["account_id"]) %>
